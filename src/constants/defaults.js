import _ from 'lodash';

export default null;

export const defaultDqRating = 5;
export const defaultNumRounds = 20;
export const defaultRefScore = 5;
export const defaultStrategyId = 11100;
export const defaultStrategy = {
	stat: 'str',
	level: 1,
	numFlags: 0,
	flag: null,
	targetStat: null,
	isFinisher: false,
};
